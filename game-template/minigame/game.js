/**
 * WeChat minigame entry for the interactive video-game template.
 * Open this folder in WeChat DevTools as a 小游戏 project (compileType: game).
 */
const { startPlayer } = require('./js/player');

startPlayer();
