{
  "name": "ai-application-factory",
  "private": true,
  "author": "houyu.hy <785798835@qq.com>",
  "license": "MIT",
  "workspaces": ["frontend"],
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "dev:desktop": "tauri dev",
    "build:mac": "tauri build --bundles app,dmg && node scripts/promote-dmg.mjs",
    "build:windows": "node scripts/build-windows.mjs && node scripts/promote-windows.mjs",
    "init:ipad": "node scripts/init-ipad.mjs",
    "build:ipad": "node scripts/build-ipad.mjs && node scripts/promote-ipad.mjs",
    "typecheck": "npm run typecheck --workspace frontend",
    "test": "npm test --workspace frontend"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
